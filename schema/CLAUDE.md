Read EVA.md at the vault root before making any change. It defines the page
types, directory layout, required frontmatter, linking rules, and the
merge-over-duplicate policy this vault is maintained under.
